# typos
alias giti='git'

# venv
alias ve='source venv/bin/activate'
alias vpip='python venv/bin/pip'

# sudo
alias fucking=sudo

# zsh
alias change='code ~/.zshrc'
alias update='exec zsh'

# ls
alias l='arch -x86_64 colorls --group-directories-first --almost-all'
alias ll='arch -x86_64 colorls --group-directories-first --almost-all --long' # detailed list view
